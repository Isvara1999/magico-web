node_modules

# Local Netlify folder
.netlify

# Build output
/dist

# Cloudflare Wrangler local dev state (local D1 sqlite, logs)
.wrangler
.dev.vars

# Vite cache metadata (can change when running dev)
node_modules/.vite/deps/_metadata.json

# Agent related
.claude/
outputs/
.graphify_python
.graphify_transcripts.json
.graphifyignore
graphify-out/

# Datos reales de huéspedes / exports de migración — nunca al repo (ver
# CLAUDE.md: scripts de fix temporales se escriben, se corren, se borran).
huespedes.csv
insert_historico.sql
update_canal.sql

